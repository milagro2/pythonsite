name: Deploy documentation

on:
  push:
    branches:
      - master

jobs:
  update_docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy MkDocs
        run: mkdocs gh-deploy
       
