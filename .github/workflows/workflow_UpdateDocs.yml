name: Update documentation
on:
  push:
    branches:
      - STEP-Configuration-Linter
jobs:
  update_docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test Workflow
        run: echo Hello World
